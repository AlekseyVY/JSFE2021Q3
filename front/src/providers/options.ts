export const options = {
  bg: 1,
  tree: 1,
  snow: true,
  music: true,
  lights: false,
  color: 'blue',
};

export interface IOptions {
  bg: number;
  tree: number;
  snow: boolean;
  music: boolean;
  lights: boolean;
  color: string;
}
